# PROJECT.md — TRIW プロジェクト現状認識（正）

> **この文書がプロジェクトの現状認識の「正」である。**
> `docs/` 以下の他の文書と矛盾する場合、この文書を優先する。
> 最終更新: 2026-07-04
> 更新責任: コードやフローに影響する変更を行った者（人間・AIエージェントを問わず）は、この文書を同時に更新すること。

---

## 1. プロジェクトのゴール（3層）

### 最終ゴール
「自分の理想のラジオ番組」を実現するWebアプリ。
- Spotifyの楽曲で構成された番組に、AI生成ナレーション（TTS音声化）を挟む
- 「ナレーション → 曲 → ナレーション → 曲」と自動再生される
- FMラジオの過剰なトークと、Spotifyの無味乾燥さの中間を狙う
- 将来的には時報・ニュース・天気などの挿入も構想
- 主な利用シーン: スマホ（運転中など）。PCでも使える

### 中間ゴール（当初のリリースライン）
テーマ入力 → AI選曲 → Spotifyプレイリスト作成、までを一発で通すこと。

### 現在のフェーズ（いまここ）
**選曲精度の基礎検証。**
DJペルソナや自由文入力をいったん退け、カード＋スライダーによる構造化入力で、
「入力条件に合った選曲を安定して出せるか」をチューニングしている段階。
選曲が合格ラインに達するまで、番組化（ナレーション・TTS・再生制御）には進まない。

---

## 2. コードの世代区分（最重要の前提知識）

このリポジトリには**2世代のフローが同居している**。混同しないこと。

### 現行（開発対象）: tune フロー
- UI: `src/app/program/page.tsx`（`/program`）
- API: `src/app/api/program/tune/route.ts`
- ロジック: `src/lib/triw/` 以下
- 入力: キーワードカード（27種、`input/cards/keywordCards.ts`）＋スライダー3軸（年代・温度・人気傾向、`input/sliders/sliderControls.ts`）
- 実験ログ: `triw-experiments/experiments.ndjson`

### レガシー（参照用・原則変更しない）: mixtape フロー
- UI: `src/app/mixtape/page.tsx`、`src/app/playlist/` ほか
- ロジック: `src/lib/openai.ts`（A〜Gのペルソナ駆動フロー）、`src/lib/evaluate.ts`、`src/lib/finalize.ts`、`src/lib/resolve.ts`
- DJペルソナデータ: `src/data/djs*.ts`（11人）
- `docs/` の設計文書の大半（design_notes、playlist_flow_design、selection-algorithm-notes 等）は**この世代の記述**である

レガシーは「動く参照実装」として残す。特に `evaluate.ts` の表記マッチ検証や
`finalize.ts` の artistPolicy には、現行フローにまだ移植されていない有用なロジックがある。

### 未実装（ディレクトリのみ存在するスタブ）
- `src/lib/triw/narration/` — ナレーション生成
- `src/lib/triw/playback/` — 再生制御・TTS
- `talkEnabled` フラグはUI→APIまで配線済みだが、どこにも作用していない

---

## 3. 現行フロー（tune）の処理系

```
入力（カード＋スライダー＋曲数/分数）
  │
  ├─ buildTuneInterpretation   カードpromptText＋スライダー段階テキストをテンプレ連結（LLM不使用）
  ├─ buildPromptPlan           カードをカテゴリ別に振り分け、musicAffectMapでtempo/energy等のヒント付加
  │
  C  runTuneCandidatesC        gpt-4o-mini (temperature 0.6) で目標×3（12〜24件）の候補をJSON生成
  D  resolveCandidatesD        Spotify検索（段階的フォールバック）。ISRC＋MusicBrainzで原盤年推定。
  │                            audio features取得。title/artistマッチ情報（title_exact等）をdebugに保持
  E  evaluateTuneTracks        スコア方式: 基準70点、URI無し-70、年代ズレ-35、人気ズレ-25〜30。
  │                            40点未満で rejected。それ以外は reservePool
  F  buildVisibleQueue         reservePoolをスコア降順ソートし上位N件を切り出し
  │
  buildEvents                  現状は曲をそのまま {type:"track"} 化するのみ
```

### 生成と評価の非対称（構造上の急所）
- **生成側（C）** に渡る条件は曖昧な自然文のみ（例:「かなり古めの時代感を優先する」）
- **評価側（E）** は具体的な年レンジ（例: era<15 → 1950–1974）を持つ
- 同じスライダー値の解釈が、生成と評価で**別々の場所に別々の粒度で**書かれている

---

## 4. 既知の課題（実験ログ21件の集約と構造的原因）

実験ログのmemoに現れた不満と、コード上の原因の対応表。
**改善はこの表を基準に優先順位を決めること。**

| # | 症状（ログ由来） | 構造的原因 |
|---|---|---|
| 1 | 同一アーティスト・同一曲が毎回頻出 | buildVisibleQueueが純粋スコア順の上位N切り出しで、アーティスト重複制限がない。実行間の多様性を担保する仕組み（除外リスト等）がゼロ。プロンプト文言での抑制は効果が薄いことを実験18–20で確認済み |
| 2 | 年代スライダーが効かない | 生成側に具体年代が渡らない（§3の非対称）。評価側の年代ズレは-35点の減点にとどまり、70+10-35=45点でrejected閾値(40)を超えて生き残る |
| 3 | アーティスト解決ミス（曲名一致・別アーティスト） | searchTrackBestEffortのフォールバックが最終的にタイトルのみ検索まで落ちる。resolveCandidatesDが保持するtitle_exact/artist_exactを、evaluateTuneTracksが参照していない |
| 4 | キーワードの質感が保証されない（例: オルタナ感のない曲、Fleetwood Macの混入） | 候補がキーワードに本当に適合するかを検証する工程がない。artist_genresは未取得・未使用。audio featuresは取得済みだがEで未使用 |
| 5 | 条件の掛け合わせで両方薄まる（オルタナ×クール等） | 全条件が自然文として並列連結され、優先度が不定。musicAffectMap（温度→tempo/energy翻訳の発想）はスライダーに未接続で、カード6種にしか対応していない |
| 6 | （運用）実験ログが自由記述のみ | 定量比較・機械判定ができない。ログ19と20が完全同一（保存の二重発火疑い）という品質問題もある |

---

## 5. 選曲の合格基準（ドラフト・未承認）

番組化フェーズへ進む条件。
**以下の閾値はエージェントが置いた初期案（ドラフト）であり、合格基準としての正式採用には人間（プロデューサー）の承認が必要。**
エージェントは閾値の初期案を置いてよいが、承認前の閾値を根拠に合格判定を下してはならない。
承認・改定されたら本節の「未承認」表記を外し、承認日を記録する。
エージェントはこの基準に対する現状値を実験ログから算出し、改善提案の根拠とすること。

同一条件で5回実行したとき:

1. **キーワード適合**: 採用曲のうちキーワードの質感に合う曲が 4/5 以上（人間評価）
2. **年代適合**: スライダーが端（レベル1 or 5）のとき、指定レンジ外の曲が 0 件
3. **解決精度**: アーティスト誤解決が 0 件
4. **多様性（実行内）**: 1回の採用リスト内で同一アーティストは最大1曲
5. **多様性（実行間）**: 5回の実行で、採用曲の重複率が50%以下

---

## 6. ロードマップ（優先順位つき）

現在のフェーズ内での優先順位。上から着手する。
**詳細は `ROADMAP.md`、タスク単位の定義は `tasks/*.md` を正とする。**

0. **実験ログの最小構造化（前提作業）** — 保存時に自動算出できる評価フィールド（誤解決数、年代違反数、実行内多様性、notFound数、条件キー、コード参照）＋保存の冪等化。**区分Aの検証はこれが前提のため、完了するまで選曲結果に影響する変更（Phase 1以降）に着手しない**。それ自体は選曲結果に影響しない区分Bの変更なので、軽い検証で完了できる（課題6の最小対応）
1. **E（評価）の検証化** — 取得済みデータの活用のみで課題2,3,4の大半に対処
   - title/artistマッチ情報による誤解決の除外
   - スライダーが端のときの年代ハード制約化（減点→reject）
   - artist_genres取得とキーワードカードへのジャンルタグ付与による質感検証
2. **F（キュー構築）の多様性制約** — 同一アーティスト上限（課題1の実行内対応）。あわせて、本格的な候補プール導入（Phase 4）の前段として**軽い実行間多様性対策を検討する**: 直近runの採用曲をプロンプトへの簡易除外リストとして渡す、キュー構築時に直近runを参照して重複曲の優先度を下げる、等。低コストで効果を測れる範囲にとどめ、効果は実験ログで確認する
3. **生成と評価の語彙統一** — スライダー各段階の具体値（年レンジ、tempo/energy記述）を単一の定義ファイルに置き、CのプロンプトとEの判定が同じ定義から生成される構造にする（課題2,5）
4. **実行間多様性の本格設計** — 候補プール拡大＋サンプリング等。Phase 2の軽量対策の実験結果を踏まえて設計する（課題1）
5. **ログ運用の充実** — 人間評価（keywordFit）の入力手段、run横断の指標比較、過去ログの扱いの決定（課題6の残り）
6. **（合格後）番組化ブリッジ** — ProgramEventに"narration"型を追加、まずテキスト表示のみの曲紹介から

---

## 7. 環境・運用の前提

- Next.js (App Router) + TypeScript。`npm ci && npm run dev`
- 選曲LLM: OpenAI（`OPENAI_MODEL`、既定 gpt-4o-mini）。`OPENAI_API_KEY` 必須
- Spotify: PKCE + Cookie認証（安定稼働中）。リダイレクトURIは 127.0.0.1:3000 前提
- デプロイ: Vercel。RunLogのVercel Blob永続化は旧README記載の構想であり未着手
- 既知の外部リスク: Spotify検索の429（レート制限）。長尺テスト時に発生実績あり
