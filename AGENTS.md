# AGENTS.md — TRIW 開発ルール（AIエージェント向け）

> この文書は、AIエージェントが人間の細かい仕様決定なしに開発を進めるためのルールを定める。
> 人間（プロデューサー）とエージェント（開発責任者）の役割分担、変更の作法、検証の義務を含む。
> `docs/開発ルール.md` および `docs/prompt_memo` の精神（2025-09版）を継承し、エージェント運用向けに全面改訂したもの。
> 最終更新: 2026-07-04

---

## 0. 最初に読むもの（必須・この順で）

1. `PROJECT.md` — 現状認識の正。特に §2（世代区分）と §4（既知の課題）
2. この `AGENTS.md` 全文
3. 着手するタスクに関連するコード（`src/lib/triw/` 以下が現行）
4. `triw-experiments/experiments.ndjson` — 直近の実験結果

`docs/` 以下の旧文書は歴史的経緯の参照用。**現行仕様として扱わないこと。**

---

## 1. 役割分担

**基本原則: 人間は細かい仕様を決めない。**
人間が担うのは「体験の違和感を言語化すること」「選曲結果を耳で評価すること」「フェーズを進めるか判断すること」の3つであり、
それをどう実現するかの仕様・設計・実装はすべてエージェントの責任範囲である。
エージェントは実装の詳細について人間に判断を求めず、体験と評価に関わる問いだけを人間に返すこと。

### 人間（プロデューサー）が決めること
- プロダクトの目的・体験の良し悪しの最終判断（「なんか違う」の言語化を含む。人間の違和感は仕様より上位の入力として扱う）
- 選曲結果の主観評価（「オルタナ感がある/ない」等、耳でしか判定できないもの）
- 合格基準（PROJECT.md §5）の閾値の**正式採用**と改定
- フェーズ移行の判断（選曲チューニング → 番組化）
- 予算に関わる判断（LLMモデル変更、外部API追加）

### エージェントが決めてよいこと（人間への事前確認不要）
- 上記以外のすべての実装詳細: 関数設計、データ構造、プロンプト文言、リファクタリング方針
- **閾値の初期案を置くこと**。ただしそれはあくまでドラフトであり、合格基準として正式採用するには人間の承認が必要。承認前の閾値を根拠に合格判定を下してはならない
- ただし §3 の変更ルールと §4 の検証義務に従うこと

### エージェントが提案し、人間の承認を待つこと
- PROJECT.md §6 ロードマップの順序変更
- レガシーコード（mixtapeフロー）の削除
- 新しい外部依存（ライブラリ、API）の追加
- 合格基準そのものの変更

---

## 2. 開発の基本方針

1. **本質に集中する。** 本質＝選曲ロジックの精度と検証可能性。UIの磨き込みやレガシーのバグ潰しは現フェーズでは行わない
2. **場当たり的な修正をしない。** 症状に対しては必ず PROJECT.md §4 の対応表で構造的原因を確認し、原因側を直す
3. **プロンプト文言の調整だけで解決しようとしない。** 実験18–20で、文言追加による多様性改善が持続しないことを確認済み。生成側の変更には必ず検証側（E/F）の裏付けを対にする
4. **生成と評価の語彙は同じ定義から。** スライダーやキーワードの解釈（年レンジ、tempo/energy等）を新設・変更するときは、Cのプロンプト生成とEの判定が**単一の定義ファイル**を参照する構造を守る。二重定義を作らない
5. **取得済みデータを先に使い切る。** 新しいデータ取得やAPI呼び出しを増やす前に、既に取れているが未使用のデータ（title/artistマッチ情報、audio features等）で解決できないか検討する
6. **429（レート制限）を悪化させない。** Spotifyへのリクエスト数が増える変更には、チャンク化・Retry-After対応・候補数の動的縮小のいずれかの緩和策を添える

---

## 3. 変更のルール

### 触ってよい領域
- `src/lib/triw/` 以下、`src/app/program/`、`src/app/api/program/`
- `triw-experiments/`（ログスキーマの改善を含む）
- `PROJECT.md`、`AGENTS.md`、新設する設計文書

### 原則触らない領域（レガシー）
- `src/lib/openai.ts`、`src/lib/evaluate.ts`、`src/lib/finalize.ts`、`src/lib/resolve.ts`
- `src/app/mixtape/`、`src/app/playlist/`、`src/app/simple-playlist/`、`src/data/djs*.ts`
- ここから現行フローへ**ロジックを移植するのは推奨**（例: evaluate.tsの表記マッチ、finalize.tsのartistPolicy）。移植元は変更せずコピーして現行側に取り込む

### 共有領域（変更は影響範囲を明記して慎重に）
- `src/lib/spotify.ts`、`src/lib/http.ts`、認証まわり（`src/app/api/auth/`）
- 認証は安定稼働中。**明確な必要がない限り触らない**

### 変更単位
- 1つの変更は1つの課題（PROJECT.md §4 の番号）に対応させる。複数課題をまたぐ変更は分割する
- 大きめの変更は、いつでも戻れるよう安定点でタグを打つ（`savepoint-YYYY-MM-DD` 慣行を継続）
- 挙動を変えずログだけ追加する観測目的の変更は、機能変更とコミットを分ける

---

## 4. 検証の義務（変更を「完了」と呼ぶ条件）

変更は影響度に応じて2つの区分に分け、検証の重さを変える。どちらに当たるか迷う場合は重い側（区分A）で扱う。

### 区分A: 選曲結果に影響する変更（重い検証）
プロンプト、候補生成、評価（E）、キュー構築（F）、スライダー/カード定義など、
出力される曲リストが変わりうる変更。以下を満たすまで完了としない。

**前提: 区分Aの検証は、実験ログの最小構造化（ROADMAP.md Phase 0 / tasks/T0）の完了を前提とする。**
**Phase 0 が未完了の間は、区分Aに該当する変更に着手しないこと。**

1. **before/after の実験実行**: 変更前後で同一入力条件の実験を各3回以上実行し、ログを保存する
2. **構造化ログへの記録**: §5 のスキーマで記録する
3. **合格基準への影響報告**: PROJECT.md §5 の各項目について、改善/悪化/不変を報告する
4. **PROJECT.md の更新**: フロー・課題表・ロードマップに影響があれば同時に更新する

### 区分B: 選曲結果に影響しない変更（軽い検証）
文書修正、型の追加・整理、ログ出力の追加、コメント、リネーム、
挙動を変えない観測目的の変更など。以下で完了としてよい。

1. **typecheck が通る**（`npx tsc --noEmit` 等）
2. **lint が通る**（設定がある場合）
3. **起動確認**（`npm run dev` が立ち上がり、対象ページがエラーなく表示される）

区分Bとして扱った変更が、後から選曲結果に影響していたと判明した場合は、
その時点で区分Aの検証を遡って実施する。

主観評価（キーワード適合の耳判定）が必要な項目は、エージェントが候補結果を整形して提示し、人間の評価入力を待つ。**エージェントは主観評価を代行して合格判定を下さない。**

---

## 5. 実験ログのスキーマ

`triw-experiments/experiments.ndjson` への保存は、従来フィールドに加えて以下の `evaluation` を必須とする。
自由記述 `memo` は継続してよいが、構造化評価の代わりにはならない。

```jsonc
{
  // --- 既存フィールド（維持） ---
  "savedAt": "...",
  "input": { /* keywords, era, temperature, popularity, mode, count... */ },
  "promptPlan": { /* ... */ },
  "prompt": { /* system, user */ },
  "candidates": [ /* ... */ ],
  "visibleQueue": [ /* ... */ ],
  "memo": "自由記述（任意）",

  // --- 追加（必須） ---
  "evaluation": {
    "keywordFit":    { "score": "ok|partial|ng", "note": "" },  // 人間評価。未評価時は null
    "eraFit":        { "score": "ok|partial|ng|na", "violations": 0 },
    "resolveErrors": { "count": 0, "items": [] },               // アーティスト誤解決
    "diversityIntra": { "maxSameArtist": 1 },                    // 実行内: 同一アーティスト最大数
    "diversityInter": { "overlapWithPrevRuns": null },           // 実行間: 直近同条件runとの採用曲重複率
    "notFoundCount": 0
  },
  "codeVersion": "gitのコミットハッシュ or タグ",
  "changeRef": "この実験が検証している変更の識別子（PR名、課題番号等）"
}
```

- `eraFit` / `resolveErrors` / `diversity*` / `notFoundCount` は**コードで自動算出できる**ため、保存時に自動記録する（実装は tasks/T0。ROADMAP.md Phase 0）
- `keywordFit` のみ人間評価。入力手段（tasks/T5）が整うまでは null で保存し、区分Aの検証報告では memo 等で人間評価を補う
- `diversityInter.overlapWithPrevRuns` は、条件キー（conditionKey: 入力条件の正規化ハッシュ）が保存されていれば分析時の後算出でもよい。conditionKey の保存は T0 の必須項目
- 保存の二重発火（ログ19/20の重複）を再発させない。保存処理には冪等性（同一runIdの重複保存防止）を持たせる（T0）

---

## 6. コミュニケーションのルール

- 人間への報告・確認はカジュアルな口調でよい。堅苦しさは不要
- 1回の確認では1つのことを聞く。選択肢を出すときは推奨案と理由を添える
- 人間の作業・手間は最小にする。ただし人間にしかできないこと（主観評価、認証操作、環境変数設定）は、**手順を具体的に指示して**依頼する
- 専門用語は避けるか、使う場合は一言の説明を添える
- 作業セッションの終わりには開発メモを残す: 何を変えたか、実験結果、次にやることの3点

---

## 7. 禁止事項

- 合格判定の代行（§4）
- レガシー領域の削除・改変（§3。移植のためのコピーは可）
- 検証なしの「改善しました」報告
- PROJECT.md と矛盾する状態の放置（コードを変えたら文書も変える）
- 実在確認できない曲・アーティストを許容する方向のプロンプト緩和
- APIキー・トークン類のコードへの直書き、ログへの出力
