# 📓 開発日誌：2025-08-11

## 🎯 今日の目標・やること
- Spotifyの候補曲選定ロジックの改善方針を確定する
- 「番組名＋概要」から動的に制約を生成する仕組みを設計
- 固定ジャンルのデフォ盛りや勝手な補完を排除
- 制約が空の場合のフォールバック設計

## 🛠 今日やったこと
- 現状の `collect` の問題点を整理（固定ジャンル優先の可能性、候補ゼロになるケース）
- LLMで生成する **制約JSONスキーマ** を確定
- サーバー側の契約（I/F）を `ai-mixtape/page.tsx` と後方互換にする方針を決定
- 制約が空のときは「番組名＋概要テキスト」をそのまま検索クエリに使うフォールバックを実装案として提示
- `collect.ts` 改修案（クエリ抽出関数とフォールバックロジック追加）を作成
- `route.ts` に `sourceText` 渡しの一行追加案を作成

## 📝 気づき・メモ
- Spotifyの `available-genre-seeds` に存在しないジャンルは除外する必要がある
- 「勝手な優先」は排除しつつも、ユーザー入力は積極的に利用して候補を集めるべき
- 制約のない場合に候補ゼロになるのはUX的に問題
- 将来的に `dryrun` モードで制約＆上位候補を事前確認できるようにすると便利

## ⏭ 次回やること
- 実装案を組み込み、動作テストを実施
- 制約JSONと実際の候補曲の傾向が一致しているか確認
- 必要に応じてスコアリングの重みやクエリ抽出ロジックを微調整
