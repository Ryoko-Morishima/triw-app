# ROADMAP.md — TRIW 開発ロードマップ

> PROJECT.md §6 の詳細版。エージェントの引き継ぎ単位となるタスクは `tasks/*.md` に分割してある。
> フェーズ順序の変更は人間（プロデューサー）の承認事項（AGENTS.md §1）。
> 最終更新: 2026-07-04

---

## 前提の整理: なぜ Phase 0 が必要か

AGENTS.md §4 は「選曲結果に影響する変更（区分A）」に構造化ログによる before/after 検証を義務づけている。
一方、当初のロードマップではログ構造化が Phase 5 に置かれており、**Phase 1 の E改善が自分の検証要件を満たせない**矛盾があった。

解決: ログ構造化を「最小限（自動算出のみ）」と「運用の充実（人間評価入力・集計）」に分割する。

- **Phase 0（前提作業）**: 保存時に機械算出できる評価フィールドの記録＋保存の冪等性。
  これ自体は選曲結果に影響しない変更（区分B）なので、軽い検証で完了でき、ブートストラップ問題が起きない
- **Phase 5**: 人間評価（keywordFit）の入力手段、ログの一覧・集計・比較ビューなどの運用面

**Phase 0 が完了するまで、区分Aに該当する変更（E/F/プロンプト等）には着手しない。**

### Phase 0 に含める最小セット（これだけで区分A検証が回る根拠つき）

| 項目 | 内容 | なぜ最小セットに入るか |
|---|---|---|
| resolveErrors | Dのdebug（title_exact/artist_exact等）から誤解決を自動カウント | 課題3の before/after 比較に必須。データは取得済みで算出のみ |
| eraFit | Eの年代レンジ判定から違反数を自動カウント（レンジ無し時は na） | 課題2の before/after 比較に必須。判定ロジックはEに既存 |
| diversityIntra | visibleQueue内の同一アーティスト最大数 | 課題1（実行内）の比較に必須。単純集計 |
| notFoundCount | D の notFound 件数 | 生成品質の副作用検知（プロンプト変更で実在性が落ちていないか） |
| conditionKey | 入力条件の正規化ハッシュ | 「同一条件のrun」を機械的に特定するため。これがあれば diversityInter（実行間重複率）は**後からでも算出できる** |
| codeVersion / changeRef | コミット参照と変更識別子 | 実験結果を変更に紐づけるため |
| 保存の冪等性 | 同一runIdの重複保存防止 | ログ19/20の重複再発防止。重複があると全指標が歪む |

### Phase 0 に含めないもの（Phase 5 へ）
- keywordFit の人間評価入力手段（それまでは null 保存＋検証報告時にmemoで補う）
- diversityInter のリアルタイム算出（conditionKey があれば分析時算出で足りる。実装してもよいが完了条件にしない）
- ログの一覧・集計・比較UI、過去ログのマイグレーション

---

## フェーズ一覧

| Phase | 内容 | 検証区分 | 依存 | 対応課題 | タスク |
|---|---|---|---|---|---|
| 0 | 実験ログの最小構造化＋冪等性 | B | なし | 6 | T0 |
| 1a | 誤解決の除外（マッチ情報の活用） | A | 0 | 3 | T1a |
| 1b | 年代ハード制約化 | A | 0 | 2 | T1b |
| 1c | キーワード質感検証（ジャンルタグ） | A | 0 | 4 | T1c |
| 2 | キュー多様性（アーティスト上限＋軽い実行間対策） | A | 0（1完了推奨） | 1 | T2 |
| 3 | 生成と評価の語彙統一 | A | 1b | 2, 5 | T3 |
| 4 | 実行間多様性の本格設計 | A | 2 | 1 | T4 |
| 5 | ログ運用の充実（人間評価入力・集計） | B/A混在 | 0 | 6 | T5 |
| 6 | 番組化ブリッジ | A | 合格基準達成＋人間のフェーズ移行承認 | — | T6 |

- Phase 1 の a/b/c は独立性が高く、並行または任意順で着手可。ただし1変更1課題の原則（AGENTS.md §3）を守り、実験は個別に取ること
- Phase 5 は Phase 0 完了後ならいつでも差し込み可（優先度は 1〜2 より下）
- Phase 6 の着手には、合格基準の正式承認（PROJECT.md §5）と人間のフェーズ移行判断の両方が必要

---

## 各フェーズの完了条件（要約）

- **Phase 0**: tune実行1回で evaluation 自動フィールドが正しく保存される。同一runIdの二重保存が発生しない。typecheck/起動確認が通る
- **Phase 1a**: 誤解決（artist不一致）の曲が visibleQueue に入らない。before/after 3回ずつで resolveErrors が減少
- **Phase 1b**: スライダー端（レベル1/5）指定時、指定レンジ外の曲が採用されない。eraFit violations が 0 になる
- **Phase 1c**: キーワード不適合曲（例: 非オルタナ）の混入が減少。人間の耳評価で確認
- **Phase 2**: 採用リスト内の同一アーティストが上限以下。直近runとの重複率が低下傾向
- **Phase 3**: スライダー/カード解釈の定義が単一ファイルに集約され、CとEが同一定義を参照。回帰実験で悪化なし
- **Phase 4**: 同一条件5回実行の採用曲重複率が基準値以下（基準は承認済みの合格基準に従う）
- **Phase 5**: keywordFit を人間が記録できる。同一条件のrunを横断比較できる
- **Phase 6**: 曲間にテキスト曲紹介が挟まった番組表示ができる（TTSはさらに後）
